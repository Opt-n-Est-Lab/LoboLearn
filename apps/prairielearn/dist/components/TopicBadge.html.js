import { html } from '@prairielearn/html';
export function TopicBadge(topic) {
    return html `<span class="badge color-${topic.color}">${topic.name}</span>`;
}
//# sourceMappingURL=TopicBadge.html.js.map